import React from 'react';

//import Main from '../js/Main';

import { shallow, mount, render } from 'enzyme';
//*******************************************************************************************************
describe('<Main />', () => {
  // Render a checkbox with label in the document
  // const main = shallow(< Main />);

  // it('+++ Mess render', () => {
  //   main.render();
  //   let input = main.find('input[name="name"]');
  //   input.value = 'Тестовое имя';
  //   main.find('input[name="SendName"]').simulate('click');
  //   expect(Main.find('div.MessBox')).toHaveLength(1);
  // });
 
});